## 何をした

<!-- 1〜2行。レビュー待ちはしないので、後から履歴を追うためのメモ -->

## 確認したこと

- [ ] `npm run check` が通る
- [ ] Vercel のプレビュー URL で動いた
- [ ] `git diff --stat` に担当外のファイルが入っていない

## 相方に知らせること

<!-- 共有ファイル（types.ts / App.tsx / package.json）を触ったなら必ず書く。
     何も無ければ「なし」 -->
なし
