# 開発ルール（2人でコンフリクトさせないための取り決め）

ハッカソン当日に**手を止めない**ことだけを目的にしたルール。
「きれいなコード」より「止まらない開発」を優先する。

---

## 0. 大原則

1. **`main` は常にデプロイできる状態に保つ。** 壊したら直すか revert。放置しない。
2. **コンフリクトは「直す」ものではなく「起こさない」もの。**
   ルールの大半は、そもそも同じ行を 2 人が触らないための配置の話。
3. **小さく、頻繁に。** 1 コミットは小さく、push は 30 分に 1 回以上。
4. **迷ったら 2 分で相談。** 15 分ハマったら必ず声を出す。

---

## 1. 担当表（当日ここを埋める）

| 役割 | GitHub | 担当ディレクトリ |
| --- | --- | --- |
| A | @TakuyaMizukami7 | `src/features/____/` |
| B | @________ | `src/features/____/` |
| 共有（触る前に一声） | 2人 | `src/shared/`, `src/App.tsx`, `server/index.ts`, `package.json`, `railway.json` |

作業ログは `docs/devlog/<github-id>.md` に**1人1ファイル**。当日ファイル名を GitHub ID にリネームする。

テーマ発表後 30 分以内にこの表を埋めて commit する。**これが実質のロック機構。**

---

## 2. ブランチとマージ

- `main` への直 push は禁止。作業は必ずブランチ。
- ブランチ名は `feat/<github-id>/<内容>` （例 `feat/takuya/photo-upload`）。
  **名前で持ち主が分かる**ことが大事。
- **Pull Request は出すが、レビュー待ちはしない。** CI が通ってプレビューURLで動いたら
  自分でマージしてよい。PR の目的はレビューではなく次の 2 つ。
  - コンフリクトの早期検知
  - Railway の PR 環境のプレビュー URL を得て、実機（スマホ）で確認する
- マージは **Squash merge** で統一（履歴が 1 行になるので後から追いやすい）。
- **ブランチの寿命は最長 60 分。** それより長くなりそうなら、動く単位に割って先にマージする。
  長生きブランチ = コンフリクトの原因そのもの。

```bash
# 作業開始
git switch main && git pull
git switch -c feat/takuya/photo-upload

# 作業中（こまめに）
git add -A && git commit -m "feat: 写真アップロードのUI"
git push -u origin HEAD

# PR 作成 → CI 通過 → プレビューURLで確認 → マージ
gh pr create --fill
gh pr merge --squash --delete-branch
```

---

## 3. ファイル所有権（いちばん効くルール）

**1 機能 = 1 ディレクトリ = 1 人が所有。**

```
src/features/photo-upload/     <- A のもの。B は開かない
src/features/result-board/     <- B のもの。A は開かない
src/shared/                    <- 共有。触る前に一声かける
server/routes/xxx.ts           <- 1 エンドポイント 1 ファイル。作った人が所有
```

- 他人のディレクトリは**読んでよいが書き換えない**。直したい所は本人に依頼する。
- 共通化したくなっても、**まず自分のディレクトリの中にコピーして書く。**
  ハッカソンでは「重複」より「共有によるコンフリクト」の方が高くつく。
  共通化は動いてからでよい（そして大抵その時間は来ない）。
- `index.ts` で re-export をまとめるのは**禁止**。全員が同じ行を触ることになる。

---

## 4. 共有ファイルの扱い

コンフリクトはほぼ全部この 4 つで起きる。構造で防ぐ。

### `src/App.tsx`
**ロジックを書かない。** 各機能のコンポーネントを並べるだけに保つ。
差分が 1 行ずつになるので、コンフリクトしてもその場で直せる。

```tsx
// これでよい
<PhotoUpload />
<ResultBoard />
```

### `server/index.ts`
**ルーティングを 1 行足すだけの場所に保つ。** 処理は `server/routes/<名前>.ts` に書く。
App.tsx とまったく同じ考え方。

```ts
app.route('/api/photo', photo)   // これを 1 行足すだけ
```

### `src/shared/types.ts`
フロントとサーバーの**契約**。
- テーマ発表後 30 分以内に 2 人で決めて commit。
- 以降は **追加のみ。** 既存フィールドのリネーム・削除は必ず口頭合意してから。
- ロジックを書かない（型と定数だけ）。

### `src/index.css`
リセットと共通トークンだけ。**機能ごとの見た目は features 配下に書く。**
ここに 2 人が追記すると必ず衝突する。

### `package.json` / `package-lock.json`
- 依存追加は**宣言してから**。「〇〇入れます」と言ってから `npm install`。
- 入れたら**すぐ commit & push**、相方はすぐ `git pull && npm install`。
- lockfile は `.gitattributes` で自動マージを止めてある。コンフリクトしたら：

```bash
# package.json を先に手で解決してから
git checkout --theirs package-lock.json   # どちらでもよい。どうせ作り直す
npm install                               # ここで正しい lockfile が再生成される
git add package.json package-lock.json
```

---

## 5. 同期のリズム

- **作業開始時**: `git switch main && git pull`
- **30 分に 1 回は push。** 手元に溜め込まない。
- **ブランチを長く持つときは** こまめに main を取り込む：

```bash
git fetch origin
git rebase origin/main      # 自分のブランチ上で解決する
```

- コンフリクト解決に **10 分以上かかりそうなら、自分の変更を捨てて書き直す方が速い**ことが多い。
  AI に書かせているなら特にそう。意地を張らない。

---

## 6. フォーマット（差分ノイズを消す）

- Prettier + oxlint。設定はリポジトリのものを使う（各自の設定で上書きしない）。
- エディタは**保存時フォーマット**をオンにする（VS Code 設定はコミット済み）。
- **他人のファイルを整形しない。** 整形だけの差分は相方の作業と全面衝突する。
- push 前に `npm run check`（型・lint・フォーマット）。

---

## 6.5 チャットをまたぐ引き継ぎ（2人 × 複数チャットの前提）

機能ごとに別のチャットで AI と開発すると、**チャットが変わるたびに AI は文脈を失う。**
毎回ゼロから説明するのは時間の無駄なので、仕組みで解決する。

| タイミング | やること | Claude Code |
| --- | --- | --- |
| チャットを開いた直後 | git log / status / types.ts / devlog を読ませて現状を把握させる | **`/catchup`** |
| 作業が一段落したら | `docs/devlog/<自分のID>.md` の一番上に 1 エントリ追記 | **`/handoff`** |
| main に入れるとき | check → commit → push → PR → CI 待ち → squash マージ | **`/ship`** |

- ログは**1人1ファイル**なので、ログ自体がコンフリクトすることは原理的に無い。
- **他人のログファイルには書かない。** 読むのは自由。
- 書くのは 3〜6 行。「やったこと / 決めたこと / 次やること / 相方への申し送り」。
- **動いていないものを「完成」と書かない。** 嘘のログは次のチャットを事故らせる。

ルール本文は [CLAUDE.md](CLAUDE.md) と [AGENTS.md](AGENTS.md) に書いてあり、
AI は毎回それを読んでから作業を始める。

## 7. AI エージェントの使い方（重要）

2 人とも AI にコードを書かせる前提。AI は**放っておくと広範囲を書き換える**ので縛る。

- 作業前に **[AGENTS.md](AGENTS.md) を読ませる**（担当外を触らせないため）。
- 毎回のプロンプトで**触ってよいディレクトリを明示**する。
  例：「`src/features/photo-upload/` の中だけを変更して。他のファイルは触らないで」
- **禁止**: 一括リファクタ、一括整形、import の並べ替え、「ついでに改善」。
- 生成させるのは **1 回 1 機能**。大きく作らせるほどコンフリクトも事故も増える。
- コミット前に `git diff --stat` を見て、**担当外のファイルが入っていないか必ず確認**する。
  入っていたら `git restore <file>` で戻す。

```bash
git diff --stat   # 触ったファイル一覧。担当外があれば戻す
```

---

## 8. コミットメッセージ

深く考えない。頭に種類だけ付ける。日本語でよい。

```
feat: 写真アップロードのUIを追加
fix: ストリームが途中で切れるのを修正
chore: 依存を追加
wip: 途中（動かない）
```

---

## 9. 詰まったときの合図

- 15 分ハマったら相方に共有する。黙って粘らない。
- `main` が壊れたら**最優先で全員が手を止めて直す**。
- どうにもならない時は最後に動いていたコミットに戻す：

```bash
git log --oneline           # 動いていたコミットを探す
git revert <commit>         # 履歴を残して打ち消す（推奨）
```

---

## 10. push 前チェック

```bash
npm run check    # 型 + lint + フォーマット
git diff --stat  # 担当外のファイルを触っていないか
```
